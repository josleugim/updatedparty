<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link rel="icon" href="../../Content/images/upfavicon.ico" type="image/png" />
    <link href="@Url.Content("~/Content/upfrontend.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content("~/Scripts/jquery-1.5.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/modernizr-1.7.min.js")" type="text/javascript"></script>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.6.4/jquery.min.js" type="text/javascript"></script>
    <script type="text/javascript" src="../../Scripts/jquery.easing.1.3.js"></script>
    <script type="text/javascript" src="../../Scripts/jsonScript.js"></script>
    <script type="text/javascript" src="../../Scripts/jQuery.tmpl.min.js"></script>
    <script type="text/javascript">
        $(function () {


            $('a[href*=#]').click(function () {

                if (location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '')
         && location.hostname == this.hostname) {

                    var $target = $(this.hash);

                    $target = $target.length && $target || $('[name=' + this.hash.slice(1) + ']');

                    if ($target.length) {

                        var targetOffset = $target.offset().top;

                        $('html,body').animate({ scrollTop: targetOffset - 49 }, 1000);

                        return false;

                    }

                }

            });

        });
    </script>
    <script type="text/javascript">
        $(function () {

            // grab the initial top offset of the navigation 
            var sticky_navigation_offset_top = $('.menu').offset().top;

            // our function that decides weather the navigation bar should have "fixed" css position or not.
            var sticky_navigation = function () {
                var scroll_top = $(window).scrollTop(); // our current vertical position from the top

                // if we've scrolled more than the navigation, change its position to fixed to stick to top, otherwise change it back to relative
                if (scroll_top > sticky_navigation_offset_top) {
                    $('.menu').css({ 'position': 'fixed', 'top': 0, 'left': 'auto' });
                    //                    $('#logoUP').css({ 'display': 'block' });
                } else {
                    $('.menu').css({ 'position': 'relative', 'left': 0 });
                    //                    $('#logoUP').css({ 'display': 'none' });
                }
            };

            // run our function on load
            sticky_navigation();

            // and run it again every time you scroll
            $(window).scroll(function () {
                sticky_navigation();
            });

            // NOT required:
            // for this demo disable all links that point to "#"
            $('a[href="#"]').click(function (event) {
                event.preventDefault();
            });

        });
    </script>
    <!-- Rating Bar -->
    <script type="text/JavaScript">
        $(document).ready(function () {
            $('#ratingdiv .undone').click(function () {
                var div = '#ratingdiv';
                $(div).html('<img src="load.gif" />');
                var postdata = "rate=" + $(this).attr('rel');
                $.ajax({ type: "POST", url: "Home/Rate", data: postdata, success: function (msg) { $(div).html(msg) }, error: function () { alert('error !!') } });
            });
            $('#ratingdiv .voted').live('click', function () {
                alert('Already Done!');
            });
        });
    </script>
    @*<script>!function (d, s, id) { var js, fjs = d.getElementsByTagName(s)[0]; if (!d.getElementById(id)) { js = d.createElement(s); js.id = id; js.src = "//platform.twitter.com/widgets.js"; fjs.parentNode.insertBefore(js, fjs); } } (document, "script", "twitter-wjs");</script>*@
    <!-- Analytics -->
    <script type="text/javascript">

        var _gaq = _gaq || [];
        _gaq.push(['_setAccount', 'UA-37701719-1']);
        _gaq.push(['_trackPageview']);

        (function () {
            var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
            ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
            var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
        })();

    </script>
</head>
<body>
    <div class="mainWrapper">
        <div style="position: fixed; top: 0; right: 50px; background-color: transparent;
            line-height: 5px">
            @if (ViewContext.HttpContext.User.Identity.IsAuthenticated)
            {
                <p>
                    <img src="../../Content/images/candado_small.png" alt="Candado" />
                    @Html.ActionLink("Ajustes", "Index", "Bar", null, new { style = "color: yellow" })</p>
            }
        </div>
        <div style="position: fixed; top: 100px; right: 0;">
            @*<p>
                <a href="https://twitter.com/updatedparty" class="twitter-follow-button" data-show-count="false"
                    data-lang="en">Follow @@updatedparty</a></p>*@
        </div>
        <div class="upheader">
            <div id="logo">
            </div>
            <div id="slogan">
                <h1 style="width: 280px; font-size: 38px">
                    ENTÉRATE <span style="font-size: 24px">EN</span> <span style="font-size: 30px">TIEMPO</span>
                    <span style="font-size: 20px">REAL</span> <span style="font-size: 20px">DE</span>
                    <span style="font-size: 30px">LA VIDA</span> NOCTURNA <span style="font-size: 20px">
                        DE</span> <span style="font-size: 30px">TU CIUDAD</span>
                </h1>
            </div>
            <div id="register">
                <a href="@Url.Action("Register", "Account")" target="_self">
                    <img style="margin: 40px 40px 10px 40px" src="../../Content/images/registerbutton.png" /></a> @*<p>@Html.Partial("_LogOnPartial")</p>*@
                <p>@Html.ActionLink("Iniciar sesión", "Logon", "Account")</p>
            </div>
            @*<h1 style="margin:0;">Updated Party !</h1>*@ @*@Html.ActionLink("Registro", "Register", "Account")*@
        </div>
        <div class="menu">
            <ul>
                <li><span id="leftArrow"></span></li>
                <li><a href="#section1">What's UP</a></li>
                <li>
                    <img src="../../Content/images/Bbolita-blanca.png" /></li>
                <li><a href="#section2">Antros y Bares</a></li>
                <li><span id="logoUP"></span></li>
                <li></li>
                <li><a href="#section3">Blog</a></li>
                <li>
                    <img src="../../Content/images/Bbolita-blanca.png" /></li>
                <li><a href="#section4">Contacto</a></li>
                <li><span id="rightArrow"></span></li>
            </ul>
            <div style="clear: both; width: 100%; height: 18px; background-color: #000; background-image: url('../Content/images/menuribbon.png');
                background-repeat: repeat-x">
            </div>
        </div>
        @RenderBody()
        @*<div class="page">
        <header>
            <div id="title">
                <h1>My MVC Application</h1>
            </div>
            <div id="logindisplay">
                @Html.Partial("_LogOnPartial")
            </div>
            <nav>
                <ul id="menu">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                </ul>
            </nav>
        </header>
        <section id="main">
            @RenderBody()
        </section>
    </div>*@
        <footer>
            @Html.ActionLink("Términos, Condiciones y Aviso de Privacidad", "AvisoPrivacidad", "Home", null, new { style = "color: gray; font-size: 14px" })
        </footer>
    </div>
</body>
</html>
